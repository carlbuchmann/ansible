---
- hosts: cloudx

  tasks:
    - name: reachability test
      ios_ping:
        provider: "{{ iosxrcli }}"
        dest: '{{ item }}'
      with_items:
        - 172.16.1.202
      ignore_errors: yes
 
    - name: view output
      debug:
        msg: "{{ ping_results }}"
   